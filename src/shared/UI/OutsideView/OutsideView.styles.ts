import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  wrapper: {
    display: 'none',
    position: 'absolute',
    zIndex: 100,
  },
  visible: {
    display: 'flex',
  },
  conteiner: {},
})
