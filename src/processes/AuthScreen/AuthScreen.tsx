import Layout from '@/shared/UI/Layout/Layout'
import React, { FC } from 'react'
import { Text } from 'react-native'

const AuthScreen: FC = () => {
  return (
    <Layout>
      <Text style={{ color: 'red' }}>AuthScreen</Text>
    </Layout>
  )
}

export default AuthScreen
